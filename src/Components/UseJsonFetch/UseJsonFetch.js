import React from 'react';
import PropTypes from 'prop-types';

function UseJsonFetch(props) {
  return (
    <div>UseJsonFetch</div>
  )
}

UseJsonFetch.propTypes = {
    props: PropTypes.any,
}

export default UseJsonFetch
